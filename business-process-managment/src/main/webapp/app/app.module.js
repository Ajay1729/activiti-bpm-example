(function() {
    'use strict';

    angular.module('bpm_app',
        ['ui.router'])
        .run(run);


      run.$inject = ['$rootScope', '$state'];
      function run($rootScope, $state) {

      }

 })();